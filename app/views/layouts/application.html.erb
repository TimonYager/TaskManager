<!DOCTYPE html>
<html>
<head>
  <title>ToDoManagerApp</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <link rel="stylesheet" type="text/css" href="/css/styles.css" />
</head>
<body>

<div class="container">
	<div id="user_nav">
		<% if user_signed_in? %>
			<p class="user_auth">
				You signed in as <%= current_user.email %>. 
				<%= link_to 'Sign out', destroy_user_session_path, method: :delete %> if it's not you.<br>
			</p>
		<% else %>
			<p class="user_auth">
				Hello! <%= link_to 'Register', new_user_registration_path %> or 
				<%= link_to 'Sign in', new_user_session_path %>.
			</p>
		<% end %>
	</div>

	<%= yield %>

</div>

</body>
</html>
